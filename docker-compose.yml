version: '3.8'

services:
  auth_service:
    build: ./auth_service
    ports:
      - "8001:8001"
    volumes:
      - ./keys:/keys

  ledger_service:
    build: ./ledger_service
    ports:
      - "8002:8002"
    volumes:
      - ./keys:/keys

  protected_service:
    build: ./protected_service
    ports:
      - "8003:8003"
    volumes:
      - ./keys:/keys
